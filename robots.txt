# robots.txt — Astrix theme
# Placed in the theme root, this file overrides Ghost's default robots.txt.
# Served as a static file (no Handlebars), so the Sitemap URL below is a
# relative path. For a fully-qualified Sitemap directive (recommended by
# search + AI crawlers), replace it with your absolute URL, e.g.
#   Sitemap: https://example.com/sitemap.xml

# --- All crawlers (mirrors Ghost's default disallow rules) -----------------
User-agent: *
Disallow: /ghost/
Disallow: /p/
Disallow: /email/
Disallow: /r/
Disallow: /members/

# --- AI / agent crawlers ---------------------------------------------------
# Astrix welcomes AI agents: each notable bot is listed explicitly so the
# policy is discoverable. Agents may fetch public content but not the admin,
# members, or email areas. To BLOCK any bot, change its group's `Allow: /`
# to `Disallow: /`.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: Google-Extended
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: CCBot
User-agent: cohere-ai
User-agent: Meta-ExternalAgent
User-agent: Bytespider
Allow: /
Disallow: /ghost/
Disallow: /members/
Disallow: /email/

Sitemap: /sitemap.xml
