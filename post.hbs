{{!< default}}

{{#post}}

{{#if @custom.show_reading_progress}}
    <div class="gh-progress" aria-hidden="true">
        <div class="gh-progress-bar" id="gh-progress-bar"></div>
    </div>
{{/if}}

<article class="gh-article {{post_class}}">

    <header class="gh-article-header">
        {{#primary_tag}}
            <div class="gh-article-tag">
                <a class="gh-tag-chip" href="{{url}}">{{name}}</a>
            </div>
        {{/primary_tag}}

        <h1 class="gh-article-title">{{title}}</h1>

        {{#if custom_excerpt}}
            <p class="gh-article-excerpt">{{custom_excerpt}}</p>
        {{/if}}

        <div class="gh-article-meta">
            <div class="gh-article-authors">
                {{#foreach authors}}
                    {{#if profile_image}}
                        <img class="gh-author-avatar" src="{{img_url profile_image size="s"}}" alt="{{name}}">
                    {{/if}}
                    <a href="{{url}}">{{name}}</a>
                {{/foreach}}
            </div>
            <span aria-hidden="true">·</span>
            <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMM YYYY"}}</time>
            <span aria-hidden="true">·</span>
            <span>{{reading_time}}</span>
        </div>
    </header>

    {{#if feature_image}}
        <figure class="gh-article-image">
            <img
                srcset="{{img_url feature_image size="m"}} 640w,
                        {{img_url feature_image size="l"}} 1024w,
                        {{img_url feature_image size="xl"}} 1920w,
                        {{img_url feature_image size="xxl"}} 2400w"
                sizes="(max-width: 1600px) 100vw, 1600px"
                src="{{img_url feature_image size="xl"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                fetchpriority="high"
            >
            {{#if feature_image_caption}}
                <figcaption>{{{feature_image_caption}}}</figcaption>
            {{/if}}
        </figure>
    {{/if}}

    <div class="gh-article-layout{{#if @custom.show_toc}} has-toc{{/if}}" id="gh-article-layout">
        {{#if @custom.show_toc}}
            <aside class="gh-toc-column">
                {{> "toc"}}
            </aside>
        {{/if}}

        <div class="gh-article-column">
            <section class="gh-content gh-canvas" id="gh-content">
                {{content}}
            </section>

            {{^if access}}
                {{> "membership-cta"}}
            {{/if}}
        </div>
    </div>

    <footer class="gh-article-footer">
        <div class="gh-article-tags">
            {{#foreach tags}}
                <a class="gh-tag-chip" href="{{url}}">{{name}}</a>
            {{/foreach}}
        </div>
    </footer>

    {{> "comments"}}

</article>

{{#if @custom.show_related_posts}}
    {{> "related-posts"}}
{{/if}}

{{/post}}
